import 'package:flutter/material.dart';

class BilleteraPage extends StatelessWidget {
  const BilleteraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Billetera')),
      body: const Center(
        child: Text('Pantalla vacía - Métodos de pago'),
      ),
    );
  }
}
